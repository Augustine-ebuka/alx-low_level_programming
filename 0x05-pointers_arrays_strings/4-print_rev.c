#include "main.h"

/**
 * print_rev - function that print revers of a string
 * @s: the pointer to the string to be reversed
 */
void print_rev(char *s)
{
	int i;
	int j;

	while (s[i] = '\0')
	{
		i++;
	}
	j = i - 1;
	int k;
	for (k = j; k >= 0; k--)
	{
		-putchar(s[k]);
	}
	_putchar('\n');
}
